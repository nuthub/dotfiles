timeout 360 '~/.local/bin/lockscreen.sh'
timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"'
timeout 1800 'loginctl suspend'
# before-sleep 'echo "$(date) before-sleep" >> ~/.swayidle.log'
# before-sleep '~/.local/bin/lockscreen.sh'
# before-sleep 'bluetoothctl power off'
# before-sleep 'nmcli radio wwan off'
# after-resume 'echo "$(date) after-resume" >> ~/.swayidle.log'
# after-resume 'bluetoothctl power on'
# after-resume 'nmcli radio wwan on && mbimcli -p -d /dev/wwan0mbim0 --quectel-set-radio-state=on'
before-sleep '~/.config/swayidle/before-sleep.sh'
after-resume '~/.config/swayidle/after-resume.sh'
